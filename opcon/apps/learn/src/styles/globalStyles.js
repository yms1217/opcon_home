import { createGlobalStyle } from 'styled-components'

export const LearningGlobalStyle = createGlobalStyle`
  .learning-page {
    padding: 24px;
    min-height: calc(100vh - 60px);
  }

  .learning-page-header {
    margin-bottom: 32px;
  }
`
